# Техническое задание

## 

---

### Общие требования

Система представляет собой HTTP REST API со следующими требованиями к бизнес-логике:

* регистрация, аутентификация и авторизация пользователей;
* система групп и прав в ней (например member, admin, owner);
* возможность заходить в группы по ссылке;
* создание групп и возможность создания задач для группы;
* создание записей о задаче и ее статусе (например todo, in work, done);
* просмотр изменений в задаче (кто поставил, кто последний изменил);

## Абстрактная схема взаимодействия с системой

Ниже представлена абстрактная бизнес-логика взаимодействия пользователя с системой:

1. Пользователь регистрируется в системе.
2. Пользователь создает группу.


# Эндпоинты
#### **Регистрация пользователя**

Хендлер: `POST /api/user/register`.

Регистрация производится по паре логин/пароль. Каждый логин должен быть уникальным.
После успешной регистрации должна происходить автоматическая аутентификация пользователя.

Формат запроса:

```json
{
  "login": "<login>",
  "password": "<password>"
}
```

Возможные коды ответа:

- `200` — пользователь успешно зарегистрирован и аутентифицирован;
- `400` — неверный формат запроса;
- `409` — логин уже занят;
- `500` — внутренняя ошибка сервера.

#### **Регистрация пользователя**

Хендлер: `PATCH /api/user/profile`.

Изменение информации о пользователе. Доступно только для аутентифицированных пользователей. Все поля не обязательны.

Формат запроса:

```json
{
  "login": "<new login>",
  "first_name": "<new first name>",
  "last_name": "<new last name>",
  "bio": "<new bio>"
}
```

Возможные коды ответа:

- `200` — пользователь успешно изменен;  
    Формат ответа:
```json
{
  "login": "<login>",
  "first_name": "<first_name>",
  "last_name": "<last_name>",
  "bio": "<bio>"
}
```

- `400` — неверный формат запроса;
- `401` — пользователь не авторизован;
- `409` — логин уже занят;
- `500` — внутренняя ошибка сервера.

